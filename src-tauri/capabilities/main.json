{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Local main window. Database, downloads and updater are exposed only through scoped Rust commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
