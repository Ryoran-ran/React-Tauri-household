{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Local main window only; no network or filesystem plugins.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
